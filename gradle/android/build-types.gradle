project.ext.set("versionMajor", 1)
project.ext.set("versionMinor", 0)
project.ext.set("versionPatch", 0)
project.ext.set("versionBuild", 0)

def versionStr = "$versionMajor.$versionMinor.$versionPatch.$versionBuild"

android {
    defaultConfig {
    }
    buildTypes {
        all {
            buildConfigField("String", "APP_VERSION", "\"$versionStr\"")
            buildConfigField("String", "BASE_API_URL", '"https://api.nytimes.com/svc/search/v2/"')
            buildConfigField("String", "BASE_URL", '"https://www.nytimes.com/"')
            buildConfigField("String", "API_KEY", '"mcta793cgAqxxzWNKe7PxxAjIMvHO8ZD"')
        }
        release {
            minifyEnabled true
            debuggable false
            buildConfigField("String", "TODO", '"TODO"')
        }

        debug {
            minifyEnabled false
            debuggable true
            buildConfigField("String", "TODO", '"TODO"')

            versionNameSuffix System.getenv("BUILD_NUMBER") != null ? "-development (BuildNo: " + System.getenv("BUILD_NUMBER") + ")" : "-development"
        }
    }
}
