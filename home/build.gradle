apply from: rootProject.file('gradle/android/android-library.gradle')

dependencies {
    implementation(
            project(':core'),
            project(":feed"),
            project(":search"),
            project(":bookmarks")
    )
}
